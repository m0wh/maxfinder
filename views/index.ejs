<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MAXFinder</title>
  <style>
    *, *:before, *:after {
      box-sizing: border-box;
    }

    body {
      font-family: "Helvetica", sans-serif;
      margin: 0;
      background-color: #000;
      color: #fff;
    }

    a {
      color: #fff;
    }

    h1 {
      text-align: center;
      margin: 40px 0;
    }

    p {
      text-align: center;
    }
  </style>
</head>
<body>
  <h1>MAXFinder</h1>
  <%
    const date = new Date()
    date.setDate(date.getDate() + 6 - date.getDay())
  %>
  <p><a href="http://localhost:3000/from/PARIS%20(intramuros)/<%= date.getFullYear() %>/<%= date.getMonth() + 1 %>/<%= date.getDate() %>">Trains depuis Paris samedi prochain</a></p>
  <p><a href="http://localhost:3000/to/MARSEILLE ST CHARLES/<%= date.getFullYear() %>/<%= date.getMonth() + 1 %>/<%= date.getDate() %>">Trains pour Marseille samedi prochain</a></p>
</body>
</html>